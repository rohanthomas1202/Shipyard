---
phase: 11-agent-activity-stream
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/context/WebSocketContext.tsx
autonomous: true
requirements:
  - STREAM-01
  - STREAM-02
gap_closure: true

must_haves:
  truths:
    - "TypeScript build passes with zero errors across the entire project"
    - "setRunError receives a string argument, not unknown"
  artifacts:
    - path: "web/src/context/WebSocketContext.tsx"
      provides: "WebSocket event bridge with correct type handling for error events"
      contains: "String(event.data.error"
  key_links:
    - from: "web/src/context/WebSocketContext.tsx"
      to: "web/src/stores/wsStore.ts"
      via: "store.setRunError(runId, string)"
      pattern: "setRunError.*String"
---

<objective>
Fix the pre-existing TypeScript build error in WebSocketContext.tsx where `event.data.error` (type `unknown`) is passed to `setRunError` which expects `string`. This is the only blocker gap from phase 11 verification.

Purpose: The build must pass with zero TypeScript errors for the phase to be complete.
Output: One-line fix in WebSocketContext.tsx, clean build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-activity-stream/11-VERIFICATION.md

<interfaces>
From web/src/stores/wsStore.ts:
```typescript
setRunError: (runId: string, error: string) => void
```

From web/src/types/index.ts:
```typescript
export interface WSEvent {
  type: string
  run_id: string
  seq: number
  node: string | null
  model: string | null
  data: Record<string, unknown>
  timestamp: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript type error in WebSocketContext.tsx error handler</name>
  <files>web/src/context/WebSocketContext.tsx</files>
  <read_first>
    - web/src/context/WebSocketContext.tsx (see line 61 where event.data.error is passed to setRunError)
    - web/src/stores/wsStore.ts (setRunError signature: (runId: string, error: string) => void)
  </read_first>
  <action>
On line 61 of web/src/context/WebSocketContext.tsx, change:

```typescript
store.setRunError(event.run_id, event.data.error)
```

to:

```typescript
store.setRunError(event.run_id, String(event.data.error ?? ''))
```

This casts the `unknown` typed `event.data.error` (from `Record<string, unknown>`) to `string` using `String()`, with a nullish coalescing fallback to empty string in case `event.data.error` is undefined. This satisfies the `setRunError(runId: string, error: string)` signature in wsStore.ts.

No other changes needed. This was introduced in commit 5083ab4 before phase 11 and is the sole remaining build error.
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard/web && npx tsc -b 2>&1 | grep "error TS" | head -5; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/context/WebSocketContext.tsx contains "String(event.data.error"
    - web/src/context/WebSocketContext.tsx does NOT contain "store.setRunError(event.run_id, event.data.error)" without String() wrapping
    - `cd web && npm run build` exits 0 with no TypeScript errors
    - `cd web && npx tsc -b 2>&1 | grep "error TS" | wc -l` outputs 0
  </acceptance_criteria>
  <done>TypeScript build passes with zero errors. setRunError receives String(event.data.error ?? '') instead of raw unknown type.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` passes with exit code 0
- `cd web && npx tsc -b 2>&1 | grep "error TS"` produces no output
- The fix is minimal: one line changed, no behavioral change, just type safety
</verification>

<success_criteria>
- Full TypeScript build passes with zero errors
- No regressions in WebSocket event handling (String() preserves the error message content)
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-activity-stream/11-03-SUMMARY.md`
</output>
