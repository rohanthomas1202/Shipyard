---
phase: 02-validation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - store/sqlite.py
  - agent/nodes/executor.py
  - agent/nodes/validator.py
  - tests/test_store.py
  - tests/test_validator_node.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "SQLite database uses WAL journal mode after initialization"
    - "Executor node runs shell commands without blocking the event loop"
    - "Validator syntax checks run without blocking the event loop"
  artifacts:
    - path: "store/sqlite.py"
      provides: "WAL mode pragma on connection open"
      contains: "PRAGMA journal_mode=WAL"
    - path: "agent/nodes/executor.py"
      provides: "Async executor using run_command_async"
      contains: "run_command_async"
    - path: "agent/nodes/validator.py"
      provides: "Async _syntax_check without subprocess.run"
      contains: "async def _syntax_check"
  key_links:
    - from: "agent/nodes/executor.py"
      to: "agent/tools/shell.py"
      via: "run_command_async import"
      pattern: "from agent.tools.shell import run_command_async"
    - from: "store/sqlite.py"
      to: "aiosqlite"
      via: "WAL pragma after connect"
      pattern: "PRAGMA journal_mode=WAL"
---

<objective>
Convert blocking subprocess calls to async and enable SQLite WAL mode.

Purpose: Executor and validator currently use synchronous subprocess.run which blocks the event loop, dropping WebSocket connections during long builds. SQLite default journal mode causes lock contention on concurrent access.

Output: Non-blocking executor node, async syntax checks in validator, WAL-mode SQLite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@agent/nodes/executor.py
@agent/nodes/validator.py
@agent/tools/shell.py
@store/sqlite.py
@agent/state.py

<interfaces>
From agent/tools/shell.py:
```python
async def run_command_async(argv: list[str], cwd: str = ".", timeout: int = 60) -> dict:
    """Non-blocking subprocess execution using argv list form (no shell=True)."""
    # Returns {"stdout": str, "stderr": str, "exit_code": int}

def run_command(command: str, cwd: str = ".", timeout: int = 60) -> dict:
    # Returns {"stdout": str, "stderr": str, "exit_code": int}
```

From agent/state.py:
```python
class AgentState(TypedDict):
    plan: list[Union[str, dict]]
    current_step: int
    working_directory: str
    error_state: Optional[str]
    edit_history: list[dict]
    invalidated_files: list[str]
    # ... other fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable SQLite WAL mode and convert executor to async</name>
  <files>store/sqlite.py, agent/nodes/executor.py, tests/test_store.py</files>
  <read_first>store/sqlite.py, agent/nodes/executor.py, agent/tools/shell.py, tests/test_store.py</read_first>
  <action>
1. In `store/sqlite.py`, method `initialize()`: Add `await self._db.execute("PRAGMA journal_mode=WAL")` and `await self._db.execute("PRAGMA synchronous=NORMAL")` immediately after the `aiosqlite.connect()` call, before `row_factory` and `executescript`. This addresses INFRA-02.

2. In `agent/nodes/executor.py`:
   - Change import from `from agent.tools.shell import run_command` to `from agent.tools.shell import run_command_async`
   - Make `executor_node` async: `async def executor_node(state: dict) -> dict:`
   - Replace `result = run_command(command, cwd=working_dir)` with `result = await run_command_async(["sh", "-c", command], cwd=working_dir)` — use `["sh", "-c", command]` to preserve shell features (pipes, redirects, env vars) while being async.
   - Keep all other logic (tracer, error handling, return values) identical.

3. In `tests/test_store.py`: Add a test `test_wal_mode_enabled` that creates a `SQLiteSessionStore` with a temp db path, calls `initialize()`, then executes `PRAGMA journal_mode` and asserts the result is `"wal"`. Clean up with `close()`.

4. In `tests/test_store.py`: If no existing async test infrastructure, add `import pytest` and `@pytest.mark.asyncio` decorator. The project uses `pytest-asyncio`.
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard && python -m pytest tests/test_store.py -x -k wal -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "PRAGMA journal_mode=WAL" store/sqlite.py returns a match
    - grep "PRAGMA synchronous=NORMAL" store/sqlite.py returns a match
    - grep "async def executor_node" agent/nodes/executor.py returns a match
    - grep "run_command_async" agent/nodes/executor.py returns a match
    - grep "subprocess.run\|run_command(" agent/nodes/executor.py returns NO matches
    - grep "test_wal_mode" tests/test_store.py returns a match
  </acceptance_criteria>
  <done>SQLite initializes with WAL mode. Executor node is async and uses run_command_async. WAL test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert validator _syntax_check to async</name>
  <files>agent/nodes/validator.py, tests/test_validator_node.py</files>
  <read_first>agent/nodes/validator.py, agent/tools/shell.py, tests/test_validator_node.py</read_first>
  <action>
1. In `agent/nodes/validator.py`:
   - Add import: `from agent.tools.shell import run_command_async`
   - Convert `_syntax_check` from sync to async: `async def _syntax_check(file_path: str) -> dict:`
   - Replace `subprocess.run(["npx", "esbuild", file_path], ...)` with `result = await run_command_async(["npx", "esbuild", file_path], timeout=30)`. Map `result["exit_code"]` for returncode check, `result["stderr"]` for error output.
   - Replace `subprocess.run(["node", "--check", file_path], ...)` with `result = await run_command_async(["node", "--check", file_path], timeout=10)`. Same mapping.
   - JSON and YAML checks remain synchronous (in-process, no subprocess) but the function is still async (they just don't await anything).
   - In `validator_node`, change `check = await asyncio.to_thread(_syntax_check, file_path)` to `check = await _syntax_check(file_path)` — no more thread wrapping needed.
   - Remove the `import subprocess` since it is no longer used after this change.

2. In `tests/test_validator_node.py`: Add a test `test_syntax_check_is_async` that verifies `_syntax_check` is a coroutine function using `import inspect; assert inspect.iscoroutinefunction(_syntax_check)`. This confirms the conversion happened.

3. Verify no remaining `subprocess.run` calls exist in `validator.py`.
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard && python -m pytest tests/test_validator_node.py -x -q && python -c "from agent.nodes.validator import _syntax_check; import inspect; assert inspect.iscoroutinefunction(_syntax_check), 'not async'"</automated>
  </verify>
  <acceptance_criteria>
    - grep "async def _syntax_check" agent/nodes/validator.py returns a match
    - grep "run_command_async" agent/nodes/validator.py returns a match
    - grep "subprocess.run" agent/nodes/validator.py returns NO matches
    - grep "asyncio.to_thread.*_syntax_check" agent/nodes/validator.py returns NO matches
    - grep "import subprocess" agent/nodes/validator.py returns NO matches
  </acceptance_criteria>
  <done>Validator _syntax_check is fully async. No subprocess.run calls remain in validator.py. Existing validator tests still pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_store.py tests/test_validator_node.py -x -q` — all tests pass
- `grep -rn "subprocess.run" agent/nodes/executor.py agent/nodes/validator.py` — returns nothing
- `grep -n "PRAGMA journal_mode=WAL" store/sqlite.py` — returns a match
</verification>

<success_criteria>
- SQLite opens in WAL mode (PRAGMA journal_mode returns 'wal')
- executor_node is async and uses run_command_async
- _syntax_check is async and uses run_command_async for TS/JS checks
- No subprocess.run calls remain in executor.py or validator.py
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-infrastructure/02-01-SUMMARY.md`
</output>
