---
phase: 11-agent-activity-stream
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/styles/glass.css
  - web/src/components/agent/EventTypeBadge.tsx
  - web/src/components/agent/EventCard.tsx
  - web/src/components/agent/StreamingBlock.tsx
  - web/src/components/agent/RunSection.tsx
  - web/src/components/agent/NewEventBadge.tsx
autonomous: true
requirements:
  - STREAM-01
user_setup: []

must_haves:
  truths:
    - "Each agent event type renders with the correct icon, badge color, and body content per the UI-SPEC event rendering contract"
    - "Stream tokens accumulate via DOM ref without triggering React re-renders per token"
    - "Events group visually by run_id with a run header showing truncated instruction text"
  artifacts:
    - path: "web/src/components/agent/EventTypeBadge.tsx"
      provides: "Colored pill badge for event type labels"
      exports: ["EventTypeBadge"]
    - path: "web/src/components/agent/EventCard.tsx"
      provides: "Single event rendering with type-specific layout, expandable detail"
      exports: ["EventCard"]
    - path: "web/src/components/agent/StreamingBlock.tsx"
      provides: "Ref-based streaming token accumulation (replaces StreamingText in stream)"
      exports: ["StreamingBlock"]
    - path: "web/src/components/agent/RunSection.tsx"
      provides: "Groups events by run_id with sticky header"
      exports: ["RunSection"]
    - path: "web/src/components/agent/NewEventBadge.tsx"
      provides: "Floating pill showing unread event count with pulse animation"
      exports: ["NewEventBadge"]
    - path: "web/src/styles/glass.css"
      provides: "slide-up and badge-pulse keyframe animations"
      contains: "@keyframes slide-up"
  key_links:
    - from: "web/src/components/agent/EventCard.tsx"
      to: "web/src/components/agent/EventTypeBadge.tsx"
      via: "import { EventTypeBadge }"
      pattern: "import.*EventTypeBadge"
    - from: "web/src/components/agent/EventCard.tsx"
      to: "web/src/components/agent/StreamingBlock.tsx"
      via: "renders StreamingBlock for stream events"
      pattern: "StreamingBlock"
    - from: "web/src/components/agent/RunSection.tsx"
      to: "web/src/components/agent/EventCard.tsx"
      via: "maps events to EventCard components"
      pattern: "EventCard"
---

<objective>
Build the event rendering primitives for the agent activity stream: EventTypeBadge, EventCard, StreamingBlock, RunSection, and NewEventBadge components, plus CSS keyframe animations.

Purpose: These components form the building blocks that Plan 02 assembles into the full ActivityStream with auto-scroll. Creating them first ensures Plan 02 has tested, typed components to wire together.

Output: Six new/modified files providing all event rendering capabilities needed by ActivityStream.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-activity-stream/11-CONTEXT.md
@.planning/phases/11-agent-activity-stream/11-UI-SPEC.md
@.planning/phases/11-agent-activity-stream/11-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing code -->

From web/src/types/index.ts:
```typescript
export interface WSEvent {
  type: string
  run_id: string
  seq: number
  node: string | null
  model: string | null
  data: Record<string, unknown>
  timestamp: number
}
```

From web/src/stores/wsStore.ts:
```typescript
// Selector: useWsStore((s) => s.agentEvents) returns WSEvent[]
// Selector: useWsStore((s) => s.activeNode) returns string | null
// Selector: useWsStore((s) => s.planSteps) returns Array<{ kind: string; label: string; status: 'pending' | 'active' | 'done' }>
```

From web/src/styles/glass.css:
```css
/* Existing keyframes: float, bounce-dot, blink */
/* Existing theme vars: --color-primary (#6366F1), --color-border, --color-text, --color-muted, --color-error (#EF4444), --color-success (#10B981), --color-warning (#F59E0B) */
/* Existing: --font-code, --radius-element (8px) */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS keyframes and create EventTypeBadge + EventCard + StreamingBlock</name>
  <files>web/src/styles/glass.css, web/src/components/agent/EventTypeBadge.tsx, web/src/components/agent/EventCard.tsx, web/src/components/agent/StreamingBlock.tsx</files>
  <read_first>
    - web/src/styles/glass.css (add keyframes after existing ones)
    - web/src/components/agent/StreamingText.tsx (ref-based token pattern to replicate)
    - web/src/types/index.ts (WSEvent interface)
    - web/src/stores/wsStore.ts (agentEvents selector pattern)
    - .planning/phases/11-agent-activity-stream/11-UI-SPEC.md (event rendering contract, color values, spacing)
  </read_first>
  <action>
**1. glass.css** -- Append two keyframes after the existing `@keyframes blink` block:

```css
@keyframes slide-up {
  from { transform: translateY(8px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

@keyframes badge-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(99, 102, 241, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(99, 102, 241, 0); }
}
```

Wrap both in `@media (prefers-reduced-motion: no-preference) { ... }` per D-03 and accessibility spec.

**2. EventTypeBadge.tsx** -- New file. Named export `EventTypeBadge({ type }: { type: string })`.

Define a module-level constant `EVENT_CONFIG` record mapping event type strings to `{ icon: string, badgeBg: string, badgeColor: string }`:

| type | icon | badgeBg | badgeColor |
|------|------|---------|------------|
| status | sync | rgba(99, 102, 241, 0.15) | #6366F1 |
| stream | auto_awesome | rgba(99, 102, 241, 0.15) | #6366F1 |
| plan_ready | checklist | rgba(99, 102, 241, 0.15) | #6366F1 |
| edit_applied | edit_document | rgba(16, 185, 129, 0.15) | #10B981 |
| exec_result | terminal | rgba(148, 163, 184, 0.15) | #94A3B8 |
| validation_result | verified | dynamic (see below) | dynamic |
| git | commit | rgba(148, 163, 184, 0.15) | #94A3B8 |
| run_completed | check_circle | rgba(16, 185, 129, 0.15) | #10B981 |
| run_failed | error | rgba(239, 68, 68, 0.15) | #EF4444 |
| run_cancelled | cancel | rgba(148, 163, 184, 0.15) | #94A3B8 |
| error | warning | rgba(239, 68, 68, 0.15) | #EF4444 |

Default fallback: `{ icon: 'info', badgeBg: 'rgba(148, 163, 184, 0.15)', badgeColor: '#94A3B8' }`.

For `validation_result`, accept an optional `passed` prop or determine from data in parent -- for the badge itself, accept `badgeBg` and `badgeColor` as optional override props so EventCard can pass dynamic colors.

Render: a `<span>` with the Material Symbols icon (14px, filled variant via `fontVariationSettings: "'FILL' 1"`) followed by a label span. Container styled as a pill: `display: inline-flex`, `alignItems: center`, `gap: 4px`, `padding: 4px 8px`, `borderRadius: 4px`, `fontSize: 11px`, `fontWeight: 600`, `lineHeight: 1.2`. Background and color from the config record. Label text is the event type with underscores replaced by spaces (e.g., `plan_ready` becomes `plan ready`).

Also export the `EVENT_CONFIG` constant and `DEFAULT_EVENT_CONFIG` so EventCard can access the `expandable` property. Add `expandable: boolean` to each config entry per the UI-SPEC table.

**3. EventCard.tsx** -- New file. Named export `EventCard({ event, animate }: { event: WSEvent; animate?: boolean })`.

Import `EventTypeBadge` and `EVENT_CONFIG`/`DEFAULT_EVENT_CONFIG` from EventTypeBadge.tsx.

Define module-level constant `NODE_LABELS`:
```typescript
const NODE_LABELS: Record<string, string> = {
  planning: 'Planning', receive: 'Receiving', reader: 'Reading files',
  editor: 'Editing', validator: 'Validating', executor: 'Running command',
  git_ops: 'Git operations', merger: 'Merging changes',
  reporter: 'Generating report', coordinator: 'Coordinating',
}
```

Layout per UI-SPEC Event Card Layout:
- Container: `<div role="article" aria-label="{type} event">` with padding 12px, border-radius `var(--radius-element)` (8px), border `1px solid var(--color-border)`, background `rgba(30, 33, 43, 0.4)`.
- For error/run_failed events: background `rgba(239, 68, 68, 0.08)`, border `1px solid rgba(239, 68, 68, 0.2)`.
- For validation_result with `event.data.passed === true`: background `rgba(16, 185, 129, 0.08)`, border `1px solid rgba(16, 185, 129, 0.2)`.
- If `animate` prop is true, apply `animation: slide-up 200ms ease-out` via inline style.
- Row 1: `<EventTypeBadge type={event.type} />` + node label (from NODE_LABELS[event.node] or event.node or empty) + timestamp (relative time helper: "just now" if <60s, "{N}m ago" if <60m, "{N}h ago" otherwise). Timestamp in muted color, 11px, pushed to right with `ml-auto`.
- Row 2 (body): Type-specific content:
  - `status`: Node label text from NODE_LABELS. If event.data.step_index is a number, show "Step {N+1}" prefix.
  - `stream`: Render nothing here (StreamingBlock handles stream tokens in RunSection).
  - `plan_ready`: "{N} steps planned" where N = (event.data.steps as unknown[]).length. Expandable: show numbered step list.
  - `edit_applied`: File path from event.data.file_path in code font (`fontFamily: var(--font-code)`, 13px).
  - `exec_result`: Command from event.data.command + exit code. Expandable: stdout/stderr in code font block with background `rgba(0, 0, 0, 0.2)`, padding 8px, borderRadius 4px.
  - `validation_result`: "Passed" or "{N} issues found". Dynamic badge colors: passed=success tint, failed=error tint. Expandable: issue list.
  - `git`: Branch or commit message from event.data.
  - `run_completed`/`run_failed`/`run_cancelled`: Label text per copywriting contract.
  - `error`: Error message from event.data.error in code font.
  - Unknown: Raw JSON.stringify(event.data, null, 2) in code font, expandable.

- Expandable detail: Only render for types where EVENT_CONFIG[type].expandable is true. Use a `<button aria-expanded={expanded} aria-controls={detailId}>` with text "Show details" / "Hide details" in muted color, 11px. Detail region: `<div id={detailId}>` with max-height transition (0 to 300px, 200ms ease-out when expanding, 150ms ease-in when collapsing). Content in code font, `rgba(0, 0, 0, 0.2)` background, 8px padding, 4px borderRadius.

Helper function `relativeTime(timestamp: number): string` -- inline in this file:
```typescript
function relativeTime(ts: number): string {
  const diff = Math.floor((Date.now() - ts) / 1000)
  if (diff < 60) return 'just now'
  if (diff < 3600) return `${Math.floor(diff / 60)}m ago`
  return `${Math.floor(diff / 3600)}h ago`
}
```

**4. StreamingBlock.tsx** -- New file. Named export `StreamingBlock({ startIndex }: { startIndex: number })`.

Replicates the ref-based pattern from StreamingText.tsx but designed for inline use in the event stream.

- Uses `useRef<HTMLSpanElement>(null)` for the text node and `useRef(startIndex)` for lastProcessed.
- Reads `useWsStore((s) => s.agentEvents)`.
- In useEffect: iterates agentEvents from lastProcessed to end, appends tokens from `stream` type events via `textRef.current.textContent += event.data.token`.
- Renders: container div with same glassmorphic card styling as EventCard (padding 12px, border-radius 8px, border, background `rgba(30, 33, 43, 0.4)`). First row: EventTypeBadge with type="stream" + "Thinking..." label in muted italic 11px. Second row: `<span ref={textRef}>` in 14px body text. Blinking cursor span (same as StreamingText: 2px wide, 14px tall, var(--color-primary) background, blink animation).
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/styles/glass.css contains "@keyframes slide-up"
    - web/src/styles/glass.css contains "@keyframes badge-pulse"
    - web/src/styles/glass.css contains "prefers-reduced-motion"
    - web/src/components/agent/EventTypeBadge.tsx contains "export function EventTypeBadge"
    - web/src/components/agent/EventTypeBadge.tsx contains "export const EVENT_CONFIG"
    - web/src/components/agent/EventCard.tsx contains "export function EventCard"
    - web/src/components/agent/EventCard.tsx contains "NODE_LABELS"
    - web/src/components/agent/EventCard.tsx contains "relativeTime"
    - web/src/components/agent/EventCard.tsx contains "aria-expanded"
    - web/src/components/agent/StreamingBlock.tsx contains "export function StreamingBlock"
    - web/src/components/agent/StreamingBlock.tsx contains "textRef"
    - web/src/components/agent/StreamingBlock.tsx contains "useRef"
    - `cd web && npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>All four files exist, export correct named exports, TypeScript build passes with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Create RunSection and NewEventBadge components</name>
  <files>web/src/components/agent/RunSection.tsx, web/src/components/agent/NewEventBadge.tsx</files>
  <read_first>
    - web/src/components/agent/EventCard.tsx (just created -- uses EventCard to render events)
    - web/src/components/agent/StreamingBlock.tsx (just created -- renders inline for stream token groups)
    - web/src/types/index.ts (WSEvent interface)
    - .planning/phases/11-agent-activity-stream/11-UI-SPEC.md (run section layout, badge layout, copywriting)
  </read_first>
  <action>
**1. RunSection.tsx** -- New file. Named export `RunSection({ runId, instruction, events }: { runId: string; instruction: string; events: WSEvent[] })`.

Per D-05: Each run gets its own section with a header.

**Run section header:**
- Only rendered when this is not the only run (pass an `isMultiRun` boolean prop, default false).
- Sticky header: text "Run: {instruction}" truncated to 60 chars with ellipsis. 11px font, semibold, uppercase, tracking-wider, muted color. Relative timestamp of first event in the run on the right.
- Bottom border: `1px solid var(--color-border)`. Padding 8px 16px.
- Background: transparent.

**Event rendering with stream coalescing:**
- Iterate over `events` array. Group consecutive `stream` events (same node) into a single StreamingBlock.
- For each event:
  - If `event.type === 'stream'`: check if the previous rendered item was also a StreamingBlock for the same node. If yes, skip (StreamingBlock picks up new tokens via its useEffect). If no, render a new `<StreamingBlock startIndex={indexOfThisEventInGlobalAgentEvents} />`.
  - Otherwise: render `<EventCard event={event} animate={isLastEvent} />` where `isLastEvent` is true only for the final event in the array (per D-03, only animate the most recent event to avoid jank).
- Gap between cards: 8px (`gap-2` in flex column).

**2. NewEventBadge.tsx** -- New file. Named export `NewEventBadge({ count, onClick }: { count: number; onClick: () => void })`.

Per D-02: Floating pill at bottom of scroll area.

- Returns null if `count <= 0`.
- Render: `<button>` element positioned `absolute`, `bottom-4`, `left-1/2`, `-translate-x-1/2`, `z-10`.
- Styling: `px-4 py-1.5 rounded-full text-xs font-semibold text-white`.
- Inline style: `background: 'rgba(99, 102, 241, 0.9)'`, `boxShadow: '0 4px 12px rgba(99, 102, 241, 0.3)'`, `animation: 'badge-pulse 2s infinite'`.
- Text: `{count} new event{count === 1 ? '' : 's'}`.
- Accessibility: `role="status"`, `aria-live="assertive"`, `aria-label="{count} new event(s), click to scroll to latest"`.
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard/web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/agent/RunSection.tsx contains "export function RunSection"
    - web/src/components/agent/RunSection.tsx contains "StreamingBlock"
    - web/src/components/agent/RunSection.tsx contains "EventCard"
    - web/src/components/agent/NewEventBadge.tsx contains "export function NewEventBadge"
    - web/src/components/agent/NewEventBadge.tsx contains "badge-pulse"
    - web/src/components/agent/NewEventBadge.tsx contains 'role="status"'
    - web/src/components/agent/NewEventBadge.tsx contains "aria-live"
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>RunSection renders events with stream coalescing, NewEventBadge shows floating pill with pulse animation, TypeScript build passes</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` passes (TypeScript compilation + Vite build)
- All six files exist with correct named exports
- EVENT_CONFIG covers all 11 event types from UI-SPEC
- NODE_LABELS covers all 10 node types from copywriting contract
- StreamingBlock uses ref-based mutation (no useState for token text)
- CSS keyframes wrapped in prefers-reduced-motion media query
</verification>

<success_criteria>
- All event rendering components compile and export correctly
- EventCard handles all event types from the UI-SPEC event rendering contract
- StreamingBlock accumulates tokens via DOM ref without per-token re-renders
- RunSection groups events and coalesces consecutive stream events into one StreamingBlock
- NewEventBadge renders pill with pulse animation and correct accessibility attributes
- CSS animations (slide-up, badge-pulse) defined in glass.css with reduced-motion support
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-activity-stream/11-01-SUMMARY.md`
</output>
