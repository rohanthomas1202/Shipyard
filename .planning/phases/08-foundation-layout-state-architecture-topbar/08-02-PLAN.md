---
phase: 08-foundation-layout-state-architecture-topbar
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/components/layout/IDELayout.tsx
  - web/src/components/layout/TopBar.tsx
  - web/src/components/layout/InstructionInput.tsx
  - web/src/components/layout/ProjectSelector.tsx
  - web/src/components/layout/RunStatusIndicator.tsx
  - web/src/components/layout/PanelHeader.tsx
  - web/src/App.tsx
  - web/src/components/agent/AgentPanel.tsx
  - web/src/components/agent/StreamingText.tsx
  - web/src/components/home/RunProgress.tsx
  - web/src/components/editor/DiffViewer.tsx
  - web/e2e/app.spec.ts
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04

must_haves:
  truths:
    - "User sees three resizable panels arranged horizontally: explorer (left), editor (center), agent (right)"
    - "User can drag panel borders to resize and sizes persist after page reload"
    - "User can collapse left panel to 48px icon rail and right panel to 0px hidden"
    - "User can expand collapsed panels via header button, double-click on handle, or keyboard shortcut"
    - "User sees a persistent top bar with instruction input, project selector dropdown, and run status indicator"
    - "Instruction input is compact by default and expands to multi-line on focus"
    - "Existing components (FileTree, AgentPanel, WorkspaceHome, DiffViewer, RunProgress) render inside new panels"
  artifacts:
    - path: "web/src/components/layout/IDELayout.tsx"
      provides: "Three-panel resizable shell replacing AppShell"
      exports: ["IDELayout"]
    - path: "web/src/components/layout/TopBar.tsx"
      provides: "Persistent top bar with instruction input, project selector, run status"
      exports: ["TopBar"]
    - path: "web/src/components/layout/InstructionInput.tsx"
      provides: "Compact-to-expanded textarea with Cmd+Enter submit"
      exports: ["InstructionInput"]
    - path: "web/src/components/layout/ProjectSelector.tsx"
      provides: "Dropdown project selector extracted from ProjectPicker"
      exports: ["ProjectSelector"]
    - path: "web/src/components/layout/RunStatusIndicator.tsx"
      provides: "Colored dot + label showing current run state"
      exports: ["RunStatusIndicator"]
    - path: "web/src/components/layout/PanelHeader.tsx"
      provides: "Reusable panel header with title + collapse toggle"
      exports: ["PanelHeader"]
    - path: "web/src/App.tsx"
      provides: "Updated provider tree with IDELayout replacing AppShell"
  key_links:
    - from: "web/src/components/layout/IDELayout.tsx"
      to: "react-resizable-panels"
      via: "Group, Panel, Separator, useDefaultLayout, usePanelRef imports"
      pattern: "import.*from 'react-resizable-panels'"
    - from: "web/src/components/layout/TopBar.tsx"
      to: "web/src/stores/wsStore.ts"
      via: "RunStatusIndicator reads snapshot from wsStore"
      pattern: "useWsStore"
    - from: "web/src/App.tsx"
      to: "web/src/components/layout/IDELayout.tsx"
      via: "IDELayout replaces AppShell"
      pattern: "import.*IDELayout"
---

<objective>
Build the three-panel resizable IDE shell with persistent top bar, replacing the current CSS grid AppShell.

Purpose: Deliver the visual VS Code-style layout foundation (LAYOUT-01 through LAYOUT-04) that all subsequent phases mount content into. Install react-resizable-panels, create all layout components per the UI-SPEC design contract, wire into App.tsx, and update E2E tests.

Output: Working IDE shell with resizable/collapsible panels, top bar with instruction input + project selector + run status, existing components mounted in new panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-layout-state-architecture-topbar/08-CONTEXT.md
@.planning/phases/08-foundation-layout-state-architecture-topbar/08-RESEARCH.md
@.planning/phases/08-foundation-layout-state-architecture-topbar/08-UI-SPEC.md
@.planning/phases/08-foundation-layout-state-architecture-topbar/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — stores the executor will consume -->

From web/src/stores/wsStore.ts:
```typescript
export const useWsStore = create<WSStore>()((set) => ({
  status: 'disconnected',
  snapshot: null,
  agentEvents: [],
  changedFiles: {},
  lastSeq: {},
  // ... actions: setStatus, setSnapshot, appendAgentEvent, etc.
}))
// Selector usage:
// useWsStore(s => s.snapshot) — for RunStatusIndicator
// useWsStore(s => s.status) — for connection banner
// useWsStore(s => s.agentEvents) — for AgentPanel
```

From web/src/stores/workspaceStore.ts:
```typescript
export const useWorkspaceStore = create<WorkspaceStore>()((set) => ({
  openTabs: [],
  activeTabId: null,
  selectedPath: null,
  // ... actions: openFile, openDiff, closeTab, setActiveTab, setSelectedPath
}))
```

From web/src/context/WebSocketContext.tsx (Plan 01 refactored):
```typescript
export function useWebSocketSend(): (data: object) => void
// No more useWebSocketContext() for state — use Zustand selectors
```

From web/src/context/ProjectContext.tsx (unchanged):
```typescript
interface ProjectContextValue {
  projects: Project[]
  currentProject: Project | null
  runs: Run[]
  currentRun: Run | null
  setCurrentProject: (project: Project | null) => void
  setCurrentRun: (run: Run | null) => void
  submitInstruction: (instruction: string, workingDir: string, context?: object) => Promise<string>
  refreshProjects: () => Promise<void>
  loading: boolean
}
```

From web/src/styles/glass.css (design tokens):
```css
--color-primary: #6366F1;
--color-background: #0F111A;
--color-surface: rgba(30, 33, 43, 0.6);
--color-border: rgba(255, 255, 255, 0.08);
--color-text: #F8FAFC;
--color-muted: #94A3B8;
--color-success: #10B981;
--color-warning: #F59E0B;
--font-ui: 'Plus Jakarta Sans', sans-serif;
--radius-panel: 16px;
--radius-element: 8px;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-resizable-panels and create layout components</name>
  <files>
    web/src/components/layout/IDELayout.tsx,
    web/src/components/layout/TopBar.tsx,
    web/src/components/layout/InstructionInput.tsx,
    web/src/components/layout/ProjectSelector.tsx,
    web/src/components/layout/RunStatusIndicator.tsx,
    web/src/components/layout/PanelHeader.tsx
  </files>
  <read_first>
    - web/src/components/layout/AppShell.tsx (current layout being replaced — understand component mounting pattern)
    - web/src/components/explorer/ProjectPicker.tsx (extraction source for ProjectSelector)
    - web/src/components/explorer/FileTree.tsx (mounted in left panel — check props interface)
    - web/src/components/agent/AgentPanel.tsx (mounted in right panel — check props)
    - web/src/components/home/WorkspaceHome.tsx (mounted in center — check props)
    - web/src/components/home/RunProgress.tsx (mounted in center — check props)
    - web/src/components/editor/DiffViewer.tsx (mounted in center — check props)
    - web/src/context/ProjectContext.tsx (useProjectContext interface)
    - web/src/hooks/useHotkeys.ts (keyboard shortcut registration)
    - web/src/styles/glass.css (design tokens)
    - web/src/stores/wsStore.ts (Plan 01 output — useWsStore for RunStatusIndicator)
    - web/package.json (verify react-resizable-panels not installed)
    - .planning/phases/08-foundation-layout-state-architecture-topbar/08-UI-SPEC.md (design contract)
    - .planning/phases/08-foundation-layout-state-architecture-topbar/08-RESEARCH.md (v4 API patterns)
  </read_first>
  <action>
    1. Install react-resizable-panels: `cd web && npm install react-resizable-panels`

    2. Create `web/src/components/layout/PanelHeader.tsx`:
       - Props: `title: string`, `onCollapse: () => void`, `collapsed?: boolean`, `collapseTooltip?: string`
       - Renders: uppercase label (11px, semibold, tracking-wider, color `var(--color-muted)`) on the left, collapse/expand chevron button on the right
       - Collapse icon: `chevron_left` for left panel, `chevron_right` for right panel — use `collapseDirection?: 'left' | 'right'` prop to determine which icon
       - When `collapsed` is true, render nothing (the icon rail in IDELayout handles collapsed state)
       - Use Material Symbols for icons (already loaded via Google Fonts CDN)
       - Styling: `flex items-center justify-between px-3 pt-3 pb-1 flex-shrink-0`

    3. Create `web/src/components/layout/InstructionInput.tsx` per D-06:
       - Props: `onSubmit: (text: string) => void`, `disabled?: boolean`
       - State: `text` (string), `expanded` (boolean)
       - Compact mode: single `textarea` with `rows={1}`, height 36px, muted border, placeholder "Describe the code change you want..."
       - Expanded mode (on focus): `rows={3}`, max-height 120px, accent border (`var(--color-primary)`), longer placeholder "Describe what you want the agent to do. Be specific about files, functions, and the expected outcome.", "Run instruction" button appears to the right
       - Submit: `Cmd+Enter` / `Ctrl+Enter` OR click "Run instruction" button
       - After submit: clear text, collapse, blur textarea
       - Disabled: 50% opacity, pointer-events none on textarea
       - Textarea background: `rgba(30, 33, 43, 0.4)`, border: `1px solid var(--color-border)` (rest) / `1px solid var(--color-primary)` (expanded)
       - Button: `px-3 py-2 rounded-lg text-sm font-semibold text-white`, background `var(--color-primary)` when enabled, `rgba(99, 102, 241, 0.3)` when disabled

    4. Create `web/src/components/layout/ProjectSelector.tsx` per D-07:
       - Read `useProjectContext()` for `projects`, `currentProject`, `setCurrentProject`, `loading`
       - Render a button showing current project name (or "Select project..." if none)
       - On click, show a dropdown list of projects below the button
       - Dropdown: `absolute`, `glass-panel` styled, `z-50`, max-height 300px overflow-auto
       - Each project item: hover state with `rgba(99, 102, 241, 0.15)` background
       - Click project to select, dropdown closes
       - Click outside to close (useEffect with document click listener)
       - Max-width 200px, text truncated with ellipsis
       - Button shows chevron_down icon from Material Symbols

    5. Create `web/src/components/layout/RunStatusIndicator.tsx` per D-07:
       - Read `useWsStore(s => s.snapshot)` for current run status
       - Also read `useProjectContext().currentRun` for run presence
       - Display: colored dot (8px circle) + status label
       - Status mapping from UI-SPEC copywriting contract:
         - No run / idle: muted text "Idle", no dot
         - `running`: warning color `var(--color-warning)`, animated dot (CSS `animate-pulse`), label "Running..."
         - `completed`: success color `var(--color-success)`, static dot, label "Completed"
         - `failed` or `error`: error color `var(--color-error)`, static dot, label "Failed"
         - `waiting_for_human`: accent color `var(--color-primary)`, pulsing dot, label "Waiting for review"
       - Dot animation: use Tailwind `animate-pulse` for running, custom CSS `animate-ping` (slower) for waiting

    6. Create `web/src/components/layout/TopBar.tsx`:
       - Per D-07, contains: ProjectSelector (left), InstructionInput (center), RunStatusIndicator (right)
       - Fixed height 52px per UI-SPEC
       - Background: `rgba(30, 33, 43, 0.8)` with `backdrop-filter: blur(24px)`, border-bottom `rgba(255, 255, 255, 0.08)`
       - Rounded top corners: `border-radius: var(--radius-panel) var(--radius-panel) 0 0`
       - Layout: flex row, items-center, gap-4, px-4
       - Left zone: ProjectSelector, flex-shrink-0
       - Center zone: InstructionInput, flex-1
       - Right zone: RunStatusIndicator, flex-shrink-0
       - Read `useProjectContext()` for `currentProject` and `submitInstruction`
       - Pass `onSubmit` to InstructionInput that calls `submitInstruction(text, currentProject.path)`
       - Disable InstructionInput when no project selected OR currentRun status is 'running' or 'waiting_for_human'
       - Show WS connection banner below top bar when `useWsStore(s => s.status) === 'disconnected'`: slim bar with text "Connection lost. Reconnecting...", background `var(--color-warning)`, 24px height

    7. Create `web/src/components/layout/IDELayout.tsx` — replaces AppShell per D-03:
       - Import from 'react-resizable-panels': `Group`, `Panel`, `Separator`, `useDefaultLayout`, `usePanelRef`
       - CRITICAL: Use v4 API names — NOT `PanelGroup`, `PanelResizeHandle`, `direction`, or `autoSaveId`
       - Structure:
         ```
         <MeshBackground />
         <SettingsModal />
         <div className="h-screen w-screen flex flex-col">
           <TopBar />
           <div className="flex-1 p-4 pt-0 overflow-hidden">
             <Group orientation="horizontal" ...useDefaultLayout props>
               <Panel ref={leftPanelRef} defaultSize={20} minSize={10} collapsible collapsedSize={3}>
                 {leftCollapsed ? <IconRail /> : <LeftPanelContent />}
               </Panel>
               <Separator />
               <Panel defaultSize={50} minSize={20}>
                 <CenterPanelContent />
               </Panel>
               <Separator />
               <Panel ref={rightPanelRef} defaultSize={30} minSize={12} collapsible collapsedSize={0}>
                 <RightPanelContent />
               </Panel>
             </Group>
           </div>
         </div>
         {rightCollapsed floating restore button}
         ```
       - Per D-05: Use `useDefaultLayout({ groupId: 'shipyard-ide-panels', storage: localStorage })` for persistence
       - Per D-04: Double-click on `Separator` toggles collapse via imperative `panelRef.current?.collapse()` / `panelRef.current?.expand()`. Add `onDoubleClick` handler on each `Separator`.
       - Track collapse state: use `onResize` callback on left/right panels to detect when `size <= collapsedSize`
       - Left panel collapsed state: show icon rail with folder icon (click to expand) and settings icon (click to open SettingsModal)
       - Right panel collapsed: show floating accent button at bottom-right per existing AppShell pattern
       - Per D-04: Register keyboard shortcuts via `useHotkeys`:
         - `Cmd+B`: toggle left panel collapse/expand
         - `Cmd+J`: toggle right panel collapse/expand
         - `Cmd+Shift+P`: focus instruction input (keep existing)
       - Center panel content: conditional on `currentRun` status (same logic as current AppShell):
         - No run: `<WorkspaceHome />`
         - `waiting_for_human`: `<DiffViewer />`
         - Running/other: `<RunProgress />`
       - Each panel body wrapped in glass-panel styling per UI-SPEC:
         - Left: `rgba(30, 33, 43, 0.6)` + blur(24px)
         - Center: `rgba(30, 33, 43, 0.4)` + blur(24px) (lower opacity per UI-SPEC)
         - Right: `rgba(30, 33, 43, 0.6)` + blur(24px)
       - Resize handle styling per UI-SPEC:
         - Rest: transparent, 12px wide hit area
         - Hover: 2px vertical line at `rgba(99, 102, 241, 0.4)` centered
         - Dragging: 2px vertical line at `#6366F1` (full accent)
         - Cursor: `col-resize`
         - Style via `data-resize-handle-active` attribute that react-resizable-panels sets, use CSS in glass.css or inline styles
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - web/node_modules/react-resizable-panels/package.json exists
    - web/src/components/layout/IDELayout.tsx contains `import { Group, Panel, Separator` from 'react-resizable-panels'
    - web/src/components/layout/IDELayout.tsx contains `useDefaultLayout`
    - web/src/components/layout/IDELayout.tsx contains `usePanelRef`
    - web/src/components/layout/IDELayout.tsx contains `orientation="horizontal"`
    - web/src/components/layout/IDELayout.tsx does NOT contain `PanelGroup` or `PanelResizeHandle` or `direction=` or `autoSaveId`
    - web/src/components/layout/TopBar.tsx contains `ProjectSelector` and `InstructionInput` and `RunStatusIndicator`
    - web/src/components/layout/InstructionInput.tsx contains `onSubmit` prop and `Cmd+Enter` / `Ctrl+Enter` handling
    - web/src/components/layout/InstructionInput.tsx contains placeholder text "Describe the code change you want..."
    - web/src/components/layout/ProjectSelector.tsx contains `useProjectContext`
    - web/src/components/layout/RunStatusIndicator.tsx contains `useWsStore`
    - web/src/components/layout/PanelHeader.tsx exports `PanelHeader`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 6 layout components created with correct react-resizable-panels v4 API, Zustand integration, UI-SPEC styling, and TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Wire IDELayout into App.tsx, migrate remaining WS consumers, update E2E tests</name>
  <files>
    web/src/App.tsx,
    web/src/components/agent/AgentPanel.tsx,
    web/src/components/agent/StreamingText.tsx,
    web/src/components/home/RunProgress.tsx,
    web/src/components/editor/DiffViewer.tsx,
    web/e2e/app.spec.ts
  </files>
  <read_first>
    - web/src/App.tsx (current provider tree — replace AppShell with IDELayout)
    - web/src/components/agent/AgentPanel.tsx (uses useWebSocketContext for status — migrate to useWsStore)
    - web/src/components/agent/StreamingText.tsx (uses useWebSocketContext subscribe — migrate to useWsStore)
    - web/src/components/home/RunProgress.tsx (uses useWebSocketContext subscribe — migrate to useWsStore)
    - web/src/components/editor/DiffViewer.tsx (uses useWebSocketContext subscribe — migrate to useWsStore)
    - web/e2e/app.spec.ts (existing tests to update for new layout)
    - web/src/stores/wsStore.ts (Plan 01 output — replacement for Context state)
    - web/src/components/layout/IDELayout.tsx (just created — verify it exports correctly)
    - .planning/phases/08-foundation-layout-state-architecture-topbar/08-UI-SPEC.md (copy contract for test assertions)
  </read_first>
  <action>
    1. Update `web/src/App.tsx`:
       - Replace `import { AppShell } from './components/layout/AppShell'` with `import { IDELayout } from './components/layout/IDELayout'`
       - Replace `<AppShell />` with `<IDELayout />`
       - Keep all providers (ErrorBoundary, WebSocketProvider, ProjectProvider) in same order
       - Do NOT delete AppShell.tsx file yet (keep for reference, it becomes dead code)

    2. Migrate `web/src/components/agent/AgentPanel.tsx`:
       - Remove `import { useWebSocketContext }`
       - Add `import { useWsStore } from '../../stores/wsStore'`
       - Replace `const { status } = useWebSocketContext()` with `const status = useWsStore(s => s.status)`
       - If it uses `subscribe` for events, replace with `const agentEvents = useWsStore(s => s.agentEvents)` and render from that array instead of accumulating via subscription

    3. Migrate `web/src/components/agent/StreamingText.tsx`:
       - Remove `import { useWebSocketContext }`
       - Add `import { useWsStore } from '../../stores/wsStore'`
       - Replace `subscribe` pattern: instead of subscribing to specific event types, use `useWsStore(s => s.agentEvents)` and filter by type. Or if the component only needs the latest event of a certain type, select with a custom selector.
       - Keep the same visual behavior — only change state source

    4. Migrate `web/src/components/home/RunProgress.tsx`:
       - Remove `import { useWebSocketContext }`
       - Add `import { useWsStore } from '../../stores/wsStore'`
       - Replace `subscribe` pattern with Zustand selector reading `agentEvents` or `snapshot`
       - The component tracks run progress events — use `useWsStore(s => s.snapshot)` for step/total and `useWsStore(s => s.agentEvents)` for event list

    5. Migrate `web/src/components/editor/DiffViewer.tsx`:
       - Remove `import { useWebSocketContext }`
       - Add `import { useWsStore } from '../../stores/wsStore'`
       - Replace `subscribe` pattern with Zustand selector. DiffViewer likely subscribes to 'diff' or 'approval' events — use `useWsStore(s => s.agentEvents)` filtered for relevant types

    6. Update `web/e2e/app.spec.ts` for new IDE layout:
       - Test "loads the home screen": Update assertion — the home screen now shows inside center panel. Check for "Ready to go" or "Select a project" text per UI-SPEC copy.
       - Test "shows the three-panel layout": Update to check for "EXPLORER" (uppercase per UI-SPEC), textarea in top bar area, "AGENT" (uppercase per UI-SPEC).
       - Test "shows agent status": Update to look for RunStatusIndicator "Idle" text per UI-SPEC.
       - Test "mesh background blobs": Keep as-is (MeshBackground unchanged).
       - Test "quick action buttons": These may now be inside WorkspaceHome in center panel — verify still accessible.
       - Test "clicking Open Project": ProjectPicker is still rendered — may need to trigger from ProjectSelector dropdown or keep Open Project button if WorkspaceHome still has it.
       - Test "prompt textarea accepts input": Textarea is now in TopBar, not center panel. Update locator if needed.
       - Test "send button": Now "Run instruction" button in TopBar. Update locator to find button with text "Run instruction".
       - Remove or update tests that assert on old layout structure that no longer exists.
       - Add new test: "top bar is visible with project selector and instruction input" — check TopBar renders with all 3 zones.
       - Add new test: "panels can be collapsed" — click collapse button in explorer header, verify panel shrinks (check for icon rail).

    7. Run full build to verify everything compiles: `cd web && npm run build`
  </action>
  <verify>
    <automated>cd /Users/rohanthomas/Shipyard/web && npm run build && npx playwright test app.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - web/src/App.tsx contains `import { IDELayout }` and does NOT contain `import { AppShell }`
    - web/src/App.tsx contains `<IDELayout />` and does NOT contain `<AppShell />`
    - web/src/components/agent/AgentPanel.tsx does NOT contain `useWebSocketContext`
    - web/src/components/agent/AgentPanel.tsx contains `useWsStore`
    - web/src/components/agent/StreamingText.tsx does NOT contain `useWebSocketContext`
    - web/src/components/home/RunProgress.tsx does NOT contain `useWebSocketContext`
    - web/src/components/editor/DiffViewer.tsx does NOT contain `useWebSocketContext`
    - web/e2e/app.spec.ts contains `EXPLORER` (uppercase panel label per UI-SPEC)
    - web/e2e/app.spec.ts contains `AGENT` (uppercase panel label per UI-SPEC)
    - `npm run build` exits 0 (Vite production build succeeds)
    - `npx playwright test app.spec.ts` exits 0 (E2E tests pass)
  </acceptance_criteria>
  <done>App.tsx wired to IDELayout, all WebSocketContext consumers migrated to Zustand, E2E tests updated for new IDE layout, production build succeeds, E2E tests pass</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` passes (TypeScript + Vite production build)
- `cd web && npx playwright test app.spec.ts` passes (E2E tests for new layout)
- `cd web && npx tsc --noEmit` passes (strict type checking)
- Three-panel layout renders with react-resizable-panels v4 API
- TopBar contains instruction input, project selector, run status indicator
- No component imports `useWebSocketContext` for state (only `useWebSocketSend` for send function)
- E2E test file references new UI-SPEC copy text
</verification>

<success_criteria>
- IDE shell renders three resizable panels (LAYOUT-01)
- Panel sizes persist to localStorage via useDefaultLayout (LAYOUT-02)
- Panels collapse/expand via header button, double-click handle, and Cmd+B/Cmd+J (LAYOUT-03)
- Top bar shows instruction input with compact/expand, project selector dropdown, run status indicator (LAYOUT-04)
- Existing components (FileTree, AgentPanel, WorkspaceHome, DiffViewer, RunProgress) render correctly in new panels
- Production build succeeds with zero TypeScript errors
- E2E tests pass for new IDE layout
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-layout-state-architecture-topbar/08-02-SUMMARY.md`
</output>
